apiVersion: v1
kind: Service
metadata:
  name: {{ include "http-pulsar-router.fullname" . }}
  labels:
    {{- include "http-pulsar-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
      {{- if and .Values.service.nodePort (or (eq .Values.service.type "NodePort") (eq .Values.service.type "LoadBalancer")) }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "http-pulsar-router.selectorLabels" . | nindent 4 }}
